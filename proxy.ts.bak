// Middleware/proxy is not supported with static export (output: 'export').
// This file is kept for reference if switching back to server-based deployment.

import type { NextRequest } from 'next/server';
import { NextResponse } from 'next/server';

export default function proxy(request: NextRequest) {
  return NextResponse.next();
}

export const config = {
    matcher: [],
};